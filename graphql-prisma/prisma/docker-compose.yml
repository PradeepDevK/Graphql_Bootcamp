version: '3'
services:
  prisma:
    image: prismagraphql/prisma:1.34
    restart: always
    ports:
    - "4466:4466"
    environment:
      PRISMA_CONFIG: |
        port: 4466
        # uncomment the next line and provide the env var PRISMA_MANAGEMENT_API_SECRET=my-secret to activate cluster security
        # managementApiSecret: my-secret
        databases:
          default:
            connector: postgres
            host: ec2-52-200-119-0.compute-1.amazonaws.com
            database: dacnsu4orlkoj1
            schema: public
            user: xnsawqlmorydxf
            password: efe0342308530d377aef866e5229618f4774b5cefc078bac0fb4cd55a3a31eca
            ssl: true
            rawAccess: true
            port: '5432'
            migrations: true